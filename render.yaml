services:
  - type: web
    name: starlink-speedtest-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python web_hosting.py --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7
